# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below
@thread
def bt_2():
  while True:
    e = At11
    if e == At11:
      e = yield{waitFor: AnyCommand, block: [Up, Left]}
    elif e == At12:
      e = yield{waitFor: AnyCommand, block: [Up, Down]}
    elif e == At13:
      e = yield{waitFor: AnyCommand, block: [Up, Right]}
    elif e == At23:
      e = yield{waitFor: AnyCommand, block: [Left, Right]}
    elif e == At33:
      e = yield{waitFor: AnyCommand, block: [Down, Right]}
    elif e == At32:
      e = yield{waitFor: AnyCommand, block: [Up, Down]}
    elif e == At31:
      e = yield{waitFor: AnyCommand, block: [Left, Down]}
    else: #if e == At21 last case
      e = yield{waitFor: AnyCommand, block: [Left, Right]}

@thread
def bt_3():
  for i in range(5):
    yield{request: AnyCommand}

@thread
def bt_4():
  while True:
    yield{waitFor: At23, mustFinish: True}
    yield{waitFor: [Up, Down]}