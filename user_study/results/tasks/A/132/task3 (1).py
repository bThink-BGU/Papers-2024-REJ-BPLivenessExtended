# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below
@thread
def bt_2():
    e = At11
    curr = 1
    for i in range(5):
      if e == A11 and curr == 2:
        yield {request: [Right], mustFinish: True}
      elif e == A12 and curr == 3:
        yield {request: [Right], mustFinish: True}
      elif e == A13 and curr == 4:
        yield {request: [Down], mustFinish: True}
      elif e == A23 and curr == 1:
        pass
      elif e == A31 and curr == 2:
        yield {request: [Right], mustFinish: True}
      elif e == A32 and curr == 3:
        yield {request: [Right], mustFinish: True}
      elif e == A33 and curr == 4:
        yield {request: [Up], mustFinish: True}
      else:
        e = yield{request: AnyCommand}

@thread
def bt_3():
    e = At11
    while True:
        if e == At11:
            e = yield {waitFor: AnyAt, block: [Up, Left]}
        elif e == At12:
            e = yield {waitFor: AnyAt, block: [Up, Down]}
        elif e == At13:
            e = yield {waitFor: AnyAt, block: [Up, Right]}
        elif e == At21:
            e = yield {waitFor: AnyAt, block: [Left, Right]}
        elif e == At23:
            e = yield {waitFor: AnyAt, block: [Left, Right]}
        elif e == At31:
            e = yield {waitFor: AnyAt, block: [Down, Left]}
        elif e == At32:
            e = yield {waitFor: AnyAt, block: [Up, Down]}
        elif e == At33:
            e = yield {waitFor: AnyAt, block: [Down, Right]}
        else:
           e = yield{waitFor: AnyAt}