# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below
def bt_2():
    e = At11
    while True:
        if e == At12 or e == At32:
            e = yield {waitFor: AnyAt, block: [Down, Up]}
        elif e == At21 or e == At23:
            e = yield {waitFor: AnyAt, block: [Left, Right]}
        elif e == At11:
            e = yield {waitFor: AnyAt, block: [Up, Left]}
        elif e == At13:
            e = yield {waitFor: AnyAt, block: [Up, Right]}
        elif e == At33:
            e = yield {waitFor: AnyAt, block: [Right, Down]}
        else: #if At31
            e = yield {waitFor: AnyAt, block: [Down, Left]}
def bt_3(): #if down 2
    yield {waitFor: At21}
    yield {waitFor: At31, request: Right}
    yield {waitFor: At32, request: Right}
    yield {waitFor: At33, request: Up}
def bt_4(): #if 2 moves done and not at 31
    yield {waitFor: AnyAt}
    yield {waitFor: AnyAt}
    yield {waitFor: [At11, At12, At13, At21, At23, At32, At33], mustFinish: True}
    yield {waitFor: AnyAt}
    yield {waitFor: At23}
    yield {waitFor: AnyAt, mustFinish: True}