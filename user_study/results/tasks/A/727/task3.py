# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below

@thread
def bt_3():
  steps = 0
  e = At11
  while True:
      if (steps == 2 or steps == 4) and e == At13:
          e =yield {request: [Down]}
          steps += 1
      elif (steps == 1 or steps == 3) and e == At12:
          e =yield {request: [Right]}
          steps += 1
      elif steps == 3 and e == At23:
          e =yield {request: [Down, Up]}
          steps += 1
      elif (steps == 2 or steps == 4) and e == At21:
          e = yield {request: [Down]}
          steps += 1
      elif steps == 2  and e == At11:
          e =yield {request: [Right]}
          steps += 1
      elif steps == 1 and e == At21:
          e = yield {request: [Down, Up]}
          steps += 1
      elif steps == 2 and e == At31:
          e = yield {request: [Right]}
          steps += 1
      elif steps == 3 and e == At32:
          e = yield {request: [Right]}
          steps += 1
      elif steps == 4 and e == At33:
          e = yield {request: [Up]}
          steps += 1
      elif steps == 0:
          e = yield {waitFor: AnyAt}
          steps += 1

@thread
def bt_2():
    e = At11
    while True:
        if e == At11:
            e = yield {waitFor: AnyAt, block: [Up, Left]}
        elif e == At13:
            e = yield {waitFor: AnyAt, block: [Up, Right]}
        elif e == At31:
            e = yield {waitFor: AnyAt, block: [Down, Left]}
        elif e == At33:
            e = yield {waitFor: AnyAt, block: [Down, Right]}
        elif e == At12 or e == At32:
            e = yield {waitFor: AnyAt, block: [Up, Down]}
        elif e == At21 or e == At23:
            e = yield {waitFor: AnyAt, block: [Right, Left]}
        else:
            e = yield {waitFor: AnyAt}