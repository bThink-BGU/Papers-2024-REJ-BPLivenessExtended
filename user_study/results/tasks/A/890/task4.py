# -*- coding: utf-8 -*-
"""task4.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1xr0ykmflWmrE8LU7N72vjRC_AkZ90BNY
"""

# Don't touch this b-thread
@thread
def bt_1():
    while True:
        yield {request: [Up, Down, Left, Right]}

# Enter your solution below
#if on R dont allow down or right

visit =0
@thread
def bt_2():
      e = At11
      while True:
        if e == At31:
          visit = visit+1
          e = yield {waitFor: AnyAt}
        if e ==At33:
          visit = visit+1
          e = yield {waitFor: AnyAt, block: [Down, Right]}
        if visit%2 ==1 and e == At23:
            e = yield {waitFor: AnyAt, block: [Down],mustFinish:True}
        if visit%2 ==1 and e == At32:
            e = yield {waitFor: AnyAt, block: [Right],mustFinish:True}
        elif visit%2 ==1:
            e = yield {waitFor: AnyAt,mustFinish:True}