# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below
@thread
def bt_2():
    e = At11
    while True:
        if e ==At23:
          e = yield {waitFor: AnyAt, block: [Left, Right],mustFinish:True}
        if e == At11:
            e = yield {waitFor: AnyAt, block: [Up, Left],mustFinish:True}
        elif e == At12 or At32:
            e = yield {waitFor: AnyAt, block: [Up, Down],mustFinish:True}
        elif e == At21:
            e = yield {waitFor: AnyAt, block: [Left, Right],mustFinish:True}
        elif e == At13 :
            e = yield {waitFor: AnyAt, block: [Up, Right],mustFinish:True}
        elif e == At33 :
            e = yield {waitFor: AnyAt, block: [Down, Right],mustFinish:True}
        elif e == At31 :
            e = yield {waitFor: AnyAt, block: [Down, Left],mustFinish:True}

@thread
def bt_3():
    for i in range(5):
        yield {request: [Up, Down, Left, Right]}