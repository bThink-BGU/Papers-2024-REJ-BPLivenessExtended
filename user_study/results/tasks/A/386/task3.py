# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below

@thread
def bt_2():
    e = At11
    while True:
        if e == At21 or e == At11 or e == At31:
            e = yield {waitFor: AnyAt, block: [Left]}
        elif e == At13 or e == At23 or e == At33:
            e = yield {waitFor: AnyAt, block: [Right]}
        else:
            e = yield {waitFor: AnyAt}

@thread
def bt_3():
    e = At11
    while True:
        if e == At11 or e == At12 or e == At13:
            e = yield {waitFor: AnyAt, block: [Up]}
        elif e == At31 or e == At32 or e == At33:
            e = yield {waitFor: AnyAt, block: [Down]}
        else:
            e = yield {waitFor: AnyAt}

@thread
def bt_4():
    e = At11
    while True:
        if e == At12:
            e = yield {waitFor: AnyAt, block: [Down]}
        elif e == At32:
            e = yield {waitFor: AnyAt, block: [Up]}
        elif e == At21:
            e = yield {waitFor: AnyAt, block: [Right]}
        elif e == At23:
            e = yield {waitFor: AnyAt, block: [Left]}
        else:
            e = yield {waitFor: AnyAt}

@thread
def bt_5():
    while True:
        yield {request: AnyCommand}

@thread
def bt_6():
    for i in range(5):
        yield {waitFor: AnyCommand, mustFinish: True}
    yield {waitFor: At23, mustFinish: True}