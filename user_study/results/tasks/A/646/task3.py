# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below
AnyAt = [At11, At12, At13, At21, At23, At31, At32, At33]

@thread
def bt_2():
  actions=[]
  act=yield{request:[Down,Right]}

  for i in range(4):
    actions.append(act)

    if Left in actions:
      if len(actions)<4:
        act=yield{request:[Right]}
      else:
        act=yield{request:[Down]}

    elif Up in actions:
      act=yield{request:Down}

    elif Down in actions:
      if Left not in actions:
        if len(actions)==3:
          act=yield{request:Up}

        elif len(actions)==2:
          act=yield{request:Right}

        elif len(actions)<2:
          act=yield{request:Down}

    elif Right in actions:
      if len(actions)<2:
        act=yield{request:[Right,Left]}
      elif len(actions)==2:
        act=yield{request:Down}

