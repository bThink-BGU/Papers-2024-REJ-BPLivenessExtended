1# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1gH7BWXmiPx3toUQkvfqFo5c3xSa3m3xY
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below
@thread
def bt_2():
    for i in range(5):
      yield {waitFor: [Up, Down, Left, Right]}
    yield{waitFor:At23, mustFinish: True}

@thread
def bt_3():
    for i in range(5):
        yield {request: [Up, Down, Left, Right]}

@thread
def bt_4():
    e = At11
    cant_vertical=[At12,At32]
    cant_horizontal=[At21,At23]
    while True:
        if e == At11:
            e = yield {waitFor: AnyAt, block: [Up,Left]}
        elif e == At31:
            e = yield {waitFor: AnyAt, block: [Down,Left]}
        elif e == At13:
            e = yield {waitFor: AnyAt, block: [Up,Right]}
        elif e == At33:
            e = yield {waitFor: AnyAt, block: [Down,Right]}
        elif e in cant_vertical:
            e = yield {waitFor: AnyAt, block: [Up,Down]}
        elif e in cant_horizontal:
            e = yield {waitFor: AnyAt, block: [Left,Right]}
        else:
            e = yield {waitFor: AnyAt}