# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below
@thread
def bt_2():
    e = At11
    b = 0
    while True:
        if e == At11:
          if b == 0:
            e = yield {waitFor: AnyAt, block: [Up, Left]}
          else:
            e = yield {waitFor: AnyAt, block: [Up, Left,Down]}
        elif e == At12:
          if b == 1:
            e = yield {waitFor: AnyAt, block: [Down, Up]}
          else:
            e = yield {waitFor: AnyAt, block: [Left, Down,Up]}
        elif e == At21:
          if b == 1:
            e = yield {waitFor: AnyAt, block: [Left, Right]}
          else:
            e = yield {waitFor: AnyAt, block: [Left, Right,Down]}
        elif e == At13:
          if b ==  2:
            e = yield {waitFor: AnyAt, block: [Right, Up]}
          else:
            e = yield {waitFor: AnyAt, block: [Right, Up,Left]}
        elif e == At23:
            e = yield {waitFor: AnyAt, block: [Right, Left]}
        elif e == At33:
            e = yield {waitFor: AnyAt, block: [Right, Down, Left]}
        elif e ==   At32:
            e = yield {waitFor: AnyAt, block: [Up, Down,Left]}
        elif e ==   At31:
            e = yield {waitFor: AnyAt, block: [Up, Left,Down]}
        b+=1
@thread
def bt_3():
  while True:
    yield {request: [Up, Down, Left, Right]}