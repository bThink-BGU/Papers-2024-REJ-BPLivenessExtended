# -*- coding: utf-8 -*-
"""task2.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1cU6_kbhswbYJbN_RYiga8-D5X-S2MD2O
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {request: [Up, Down, Left, Right]}

# Enter your solution below


def bt_2():
    At_Vertical = [At12,At32]  # block Up, Down;
    At_Horizontal = [At21, At23] # block Left,Right;
    At_Corner1 = [At11]# block Up, Left;
    At_Corner2 = [At13] # block Right, Up ;
    At_Corner3= [At31] # block Left, Down;
    At_Corner4 = [At44] # block Right, Down;
    e=At11
    while True:
        e=A11
        if e in At_Vertical:
            e = yield {waitFor:AtAny, block: [Up, Down]}
        elif e in At_Horizontal:
            e = yield {waitFor:AnyAt, block: [ Left, Right]}
        elif e in At_Corner1:
           e = yield {waitFor:AnyAt, block: [ Left, Up]}
        elif e in At_Corner2:
          e = yield {waitFor:AnyAt, block: [ Right, Up]}
        elif e in At_Corner3:
          e = yield {waitFor:AnyAt, block: [ Left, Down]}
        elif e in At_Corner4:
          e = yield {waitFor:AnyAt, block: [ Right, Down]}