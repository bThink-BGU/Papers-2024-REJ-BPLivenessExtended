# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below
def bt_2():
  for i in range(5):
    yield {request: [Up, Down, Left, Right]}

def bt_3():
  e = At11
  backtrack = 1
  while True:
    if e == At21:
        e = yield {waitFor: AnyAt, block: [Up, Left, Right]}
    elif e == At31:
      yield {waitFor: AnyAt, block: [Up, Down, Left]}
    elif e == At32:
        e = yield {waitFor: AnyAt, block: [Up, Down, Left]}
    elif e == At33:
        e = yield {waitFor: AnyAt, block: [Left, Down, Right]}
    elif e == At12:
        if backtrack == 1:
          e = yield {waitFor: AnyAt, block: [Up, Down]}
          backrack = 0
        else:
          e = yield {waitFor: AnyAt, block: [Up, Down, Right]}
    elif e == At13:
        if backtrack == 1:
          e = yield {waitFor: AnyAt, block: [Up,Right]}
          backrack = 0
        else:
          e = yield {waitFor: AnyAt, block: [Up, Left, Right]}
    elif e == At11:
        if backtrack == 1:
          e = yield {waitFor: AnyAt, block: [Up, Left]}
          backrack = 0
        else:
          e = yield {waitFor: AnyAt, block: [Up, Down, Left]}