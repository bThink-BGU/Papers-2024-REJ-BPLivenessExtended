# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below
@thread
def bt_2():
    e = At11
    while True:
        if e == At12 or e == At32:
            e = yield {waitFor: AnyAt, block: [Up, Down]}
        elif e == At21 or e == At23:
            e = yield {waitFor: AnyAt, block: [Left, Right]}
        elif e == At13:
            e = yield {waitFor: AnyAt, block: [Up, Right]}
        elif e == At31:
            e = yield {waitFor: AnyAt, block: [Down, Left]}
        elif e == At33:
            e = yield {waitFor: AnyAt, block: [Down, Right]}
        else:
            e = yield {waitFor: AnyAt, block: [Up, Left]}

@thread
def bt_3():
    for j in range(5):
        yield {request: [Down, Up, Left, Right]}
        e = yield {waitFor: AnyAt}
        if e == At31:
            yield {request: Right}
            yield {request: Right}
            yield {request: Up}
        elif e == At11 and j == 1:
            yield {request: Right}
            yield {request: Right}
            yield {request: Down}
        elif e == At23 and j == 2:
            yield {request: [Down, Up]}
            command = yield {waitFor: [Down, Up]}
            if command == Down:
              yield {request: [Up]}
            else:
                yield {request: [Down]}