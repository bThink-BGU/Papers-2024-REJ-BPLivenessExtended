# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below
def bt_2():
  i = 5
  e = yield {waitFor: AnyAt, request: [Right, Down]}
  i-=1
  if i == 4:
    if e == At12:
      e = yield{waitFor: AnyAt, request:[Left, Right]}
    elif e == At21:
      e = yield{waitFor: AnyAt, request:[Up, Down]}
    i-=1
  if i==3:
    if e == At13:
      e = yield{waitFor: AnyAt, request:[Left, Down]}
      i-=1
    elif e == At11:
      yield{request:[Right]}
      yield{request:[Right]}
      yield{request:[Down]}
      i=0
    elif e == At31:
      yield{request:[Right]}
      yield{request:[Right]}
      yield{request:[Up]}
      i=0
    elif e == At11:
      yield{request:[Right]}
      yield{request:[Right]}
      yield{request:[Down]}
      i=0

  if i==2:
    if e == At12:
      yield{request:[Right]}
      yield{request:[Down]}
      i=0
    elif e == At23:
      e = yield{waitFor: AnyAt, request: [Up, Down]}
      i-=1

  if i==1:
    if e == At13:
      yield{request:[Down]}
      i=0
    elif e == At33:
      yield{request:[Up]}
      i=0