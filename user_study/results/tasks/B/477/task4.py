# -*- coding: utf-8 -*-
"""task4.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1xr0ykmflWmrE8LU7N72vjRC_AkZ90BNY
"""

# Don't touch this b-thread
@thread
def bt_1():
    while True:
        yield {request: [Up, Down, Left, Right]}

# Enter your solution below
@thread
def bt_2():
  e = At11
  reached = At11
  while True:
    if reached == At11:
      e = yield {waitFor: AnyAt}
      while e == At33:
        e = yield {waitFor: AnyAt}
        reached = e
    else:
      if reached == At32 and e == At32:
        e = yield {waitFor: AnyAt, block: [Right]}
        while e == At33:
          e = yield {waitFor: AnyAt}
          reached = e
      if reached == At23 and e == At23:
        e = yield {waitFor: AnyAt, block: [Down]}
        while e == At33:
          e = yield {waitFor: AnyAt}
          reached = e
      else:
        e = yield {waitFor: AnyAt}
        while e == At33:
          e = yield {waitFor: AnyAt}
          reached = e