# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below
@thread
def bt_2():
    e = yield {waitFor: AnyAt}

    if e == At11:
      e = yield {block: [Down], waitFor: AnyAt}
    if e == At11:
      yield {block: [Left, Down, Up]}
      yield {block: [Left, Down, Up]}
      yield {block: [Right, Left, Up]}

    if (e == At12):
      e = yield {block: [Up, Down], waitFor:AnyAt}
      if (e == At11):
        yield {block: [Left, Down, Up]}
        yield {block: [Left, Down, Up]}
        yield {block: [Right, Left, Up]}
      else:
        e = yield {waitFor: AnyAt}
        if e == At13:
          e = yield {block: [Left], waitFor: AnyAt}
          yield {block: [Left, Down, Up, Right]}
        elif e == At23:
          yield {block: [Left, Right, Up, Down]}
          yield {block: [Left, Right, Up, Down]}
        else:
          yield {block: [Left, Up, Down]}
          yield {block: [Left, Right, Up]}
    else:
      e = yield {block: [Left, Right], waitFor: AnyAt}
      if e == At31:
        yield {block: [Left, Down, Up]}
        yield {block: [Left, Down, Up]}
        yield {block: [Left, Right, Down]}
      else:
        yield {block: [Left, Down, Up]}
        yield {block: [Left, Down, Up]}
        yield {block: [Right, Left, Up]}