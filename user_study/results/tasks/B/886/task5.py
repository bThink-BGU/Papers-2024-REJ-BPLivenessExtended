# -*- coding: utf-8 -*-
"""task5.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1vb3qKAz39EH_H0m12i7lmntt4dWuQLlU
"""

# Don't touch this b-thread
@thread
def bt_1():
    while True:
        yield {request: [Up, Down, Left, Right]}

# Enter your solution below
@thread
def bt_2():
    e = At11
    counterR=0
    while True:
        if e == At11 and  needB:
            e = yield {waitFor: AnyAt}
        elif e == At21 and  needB:
            e = yield {waitFor: AnyAt}
        elif e == At31:
            needB=False
            e = yield {waitFor: AnyAt}
        elif e == At12:
            e = yield {waitFor: AnyAt}
        elif e == At13:
            e = yield {waitFor: AnyAt}
        elif e == At23 :
            e = yield {waitFor: AnyAt}
        elif e == At23 and  counterR>0 :
            e = yield {waitFor: AnyAt, block: [Down]}
        elif e == At32 and  counterR>0 :
            e = yield {waitFor: AnyAt, block: [Right]}
        elif e == At23:
            e = yield {waitFor: AnyAt, block: [Down]}
        elif e == At33:
            counterR=counterR+1
            e = yield {waitFor: AnyAt, block: [Down,Right]}
        elif e == At32 :
            e = yield {waitFor: AnyAt}
        else:
            e = yield {waitFor: AnyAt}