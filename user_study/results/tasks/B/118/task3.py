# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below

@thread
def bt_2():
  e = At11
  while True:
    if e == At11:
      e = yield {waitFor: AnyAt, block: [Right, Up]}
    elif e == At21:
      e = yield {waitFor: AnyAt, block: [Right, Left]}
    elif e == At31:
      e = yield {waitFor: AnyAt, block: [Right, Down]}
    elif e == At32:
      e = yield {waitFor: AnyAt, block: [Up, Down]}
    elif e == At33:
      e = yield {waitFor: AnyAt, block: [Down, Left]}
    elif e == At23:
      e = yield {waitFor: AnyAt, block: [Right, Left]}
    elif e == At13:
      e = yield {waitFor: AnyAt, block: [Up, Left]}
    else:
      e = yield {waitFor: AnyAt, block: [Down, Up]}


@thread
def bt_3():
   while True:
        yield {request: [Up, Down, Left, Right]}

@thread
def bt_4():
  yield {waitFor: At31}
  yield {waitFor: AnyAt, block: [Right, Up, Down]}
  yield {waitFor: AnyAt, block: [Right, Up, Down]}
  yield {waitFor: AnyAt, block: [Right, Left, Down]}

@thread
def bt_4():
  l = [] #list of places already bin to
  e = At11
  while True:
    if e not in l:
      l.append(e)
    else:
      if e == At11:
        e =  yield {waitFor: AnyAt, block: [Left, Up, Down]}
      if e == At12:
        e = yield {waitFor: AnyAt, block: [Left, Up, Down]}
      if e == At13:
        e = yield {waitFor: AnyAt, block: [Left, Up, Right]}
      if e == At33:
        e = yield {waitFor: AnyAt, block: [Left, Down, Right]}