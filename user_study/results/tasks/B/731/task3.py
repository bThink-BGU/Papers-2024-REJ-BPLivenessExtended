# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below
def bt_2():
   e = At11
   for i in range(5):
      if i == 2 and e == At11:
        e = yield {waitFor: AnyAt, request: [Right]}
      if i == 3 and e == At31:
        e = yield {waitFor: AnyAt, request: [Right]}
      if i == 3 and e == At12:
        e = yield {waitFor: AnyAt, request: [Right]}
      if i == 4 and e == At13:
        e = yield {waitFor: AnyAt, request: [Down]}
      if i == 4 and e == At33:
        e = yield {waitFor: AnyAt, request: [Up]}
      else:
        e = yield {request: [Up, Down, Left, Right]}

@thread
def bt_3():
    e = At11
    while True:
        if e == At21  or e == At23:
            e = yield {waitFor: AnyAt, block: [Left, Right]}
        elif e == At12 or e == At32:
            e = yield {waitFor: AnyAt, block: [Down, Up]}
        elif e == At31:
            e = yield {waitFor: AnyAt, block: [Down, Left]}
        elif e == At11:
            e = yield {waitFor: AnyAt, block: [Up, Left]}
        elif e == At13:
            e = yield {waitFor: AnyAt, block: [Up, Right]}
        elif e == At33:
            e = yield {waitFor: AnyAt, block: [Down, Right]}