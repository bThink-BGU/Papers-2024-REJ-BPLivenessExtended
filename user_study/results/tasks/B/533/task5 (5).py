# -*- coding: utf-8 -*-
"""task1.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1d_z0MZtTzD811ztjAOUM2a0yL_ASrZrG
"""

@thread
def bt_1():
    while True:
        yield {request: [Up, Down, Left, Right]}

# Enter your solution below
@thread
def bt_2():
    e = At11
    while True:
        if e == At11:
            e = yield {waitFor: AnyAt, block: [Up,Left]}
        elif e == At12:
            e = yield {waitFor: AnyAt, block: [Up,Down]}
        elif e == At13:
            e = yield {waitFor: AnyAt, block: [Up,Right]}
        elif e == At23:
            e = yield {waitFor: AnyAt, block: [Left,Right]}
        elif e == At33:
            e = yield {waitFor: AnyAt, block: [Right,Down]}
        elif e == At32:
            e = yield {waitFor: AnyAt, block: [Up,Down]}
        elif e == At31:
            e = yield {waitFor: AnyAt, block: [Left,Down]}
        elif e == At21:
            e = yield {waitFor: AnyAt, block: [Right,Left]}
        else:
            e = yield {waitFor: AnyAt}

@thread
def bt_3():
    visited_G = False
    visited_R_once = False

    while True:
        if visited_R_once and visited_G:
            yield {waitFor: AnyAt}
        elif not visited_G:
            yield {waitFor: AnyAt, block: [Up, Right]}
        elif not visited_R_once:
            yield {waitFor: AnyAt, block: [Down, Left]}
        else:
          yield {waitFor: AnyAt}