# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below

@thread
def bt_2():
    yield {request: [Down, Right]}  #  just for the first one


def bt_3():
    e = At11
    if e == At12:
        yield {request: [Right]}
        yield {request: [Down]}
        yield {request: [Up]}
        yield {request: [Down]}
    elif e == At21:   #  in this path(first down) there is only one way
          yield {request: [Down]}
          yield {request: [Right]}
          yield {request: [Right]}
          yield {request: [Up]}
    else:
          e = yield {waitFor: AnyAt}