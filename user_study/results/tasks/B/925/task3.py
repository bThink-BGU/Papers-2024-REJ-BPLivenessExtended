# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below
@thread
def bt_init_down():
    yield {request: Down}
    yield {request: Up}

@thread
def bt_init_right():
    yield {request: Right}
    yield {request: Down}

@thread
def bt_possible_path():
    e = At11
    while True:
        # block movement to upper bound
        if e == At11:
            e = yield {waitFor: [Down, Right]}
        # block movement to (2,2)
        elif e in [At12, At31, At32]:
            e = yield {waitFor: Right}
            yield {request: Right}
        elif e in [At13, At21]:
            e = yield {waitFor: Down}
            yield {request: Down}
        elif e == At23:
            e = yield {waitFor: [Up, Down]}
        elif e == At33:
            e = yield {waitFor: Up}
            yield {request: Up}
        else:
            e = yield {waitFor: AnyAt}

# from previous task
@thread
def bt_upper():
    e = At11
    while True:
        # block movement to upper bound
        if e in [At11, At13]:
            e = yield {waitFor: AnyAt, block: Up}
        # block movement to (2,2)
        elif e == At12:
            e = yield {waitFor: AnyAt, block: [Up, Down]}
        else:
            e = yield {waitFor: AnyAt}

@thread
def bt_lower():
    e = At11
    while True:
        # block movement to lower bound
        if e in [At31, At33]:
            e = yield {waitFor: AnyAt, block: Down}
        # block movement to (2,2)
        elif e == At32:
            e = yield {waitFor: AnyAt, block: [Up, Down]}
        else:
            e = yield {waitFor: AnyAt}

@thread
def bt_left():
    e = At11
    while True:
        # block movement to left bound
        if e in [At11, At31]:
            e = yield {waitFor: AnyAt, block: Left}
        # block movement to (2,2)
        elif e == At21:
            e = yield {waitFor: AnyAt, block: [Left, Right]}
        else:
            e = yield {waitFor: AnyAt}

@thread
def bt_right():
    e = At11
    while True:
        # block movement to right bound
        if e in [At13, At33]:
            e = yield {waitFor: AnyAt, block: Right}
        # block movement to (2,2)
        elif e == At23:
            e = yield {waitFor: AnyAt, block: [Left, Right]}
        else:
            e = yield {waitFor: AnyAt}