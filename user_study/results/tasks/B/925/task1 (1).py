# -*- coding: utf-8 -*-
"""task1.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1d_z0MZtTzD811ztjAOUM2a0yL_ASrZrG
"""

# Don't touch this b-thread
@thread
def bt_1():
    for i in range(5):
        yield {request: [Up, Down, Left, Right]}

# Enter your solution below
@thread
def bt_2():
    e = At11
    while True:
        # when standing one block above (3,1), don't allow Down operation
        if e == At21:
            e = yield {waitFor: AnyAt, block: Down}
        # when standing one block in the right of (3,1), don't allow Left operation
        elif e == At32:
            e = yield {waitFor: AnyAt, block: Left}
        else:
            e = yield {waitFor: AnyAt}