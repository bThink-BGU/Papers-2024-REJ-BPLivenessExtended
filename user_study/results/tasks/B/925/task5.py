# -*- coding: utf-8 -*-
"""task5.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1vb3qKAz39EH_H0m12i7lmntt4dWuQLlU
"""

# Don't touch this b-thread
@thread
def bt_1():
    while True:
        yield {request: [Up, Down, Left, Right]}

# Enter your solution below
visited_b = False
visited_g = False

@thread
def bt_2():
    e = At11
    while True:
        # don't allow moving to wall in this cell
        if e == At31:
            visited_b = True
            e = yield {waitFor: AnyAt, block: [Left, Down]}
        # when standing one next to (3,1), don't allow moving back to B if already been there
        elif (e == At21) and visited_b:
            e = yield {waitFor: AnyAt, block: Down}
        elif (e == At32) and not visited_b:
            e = yield {waitFor: AnyAt, block: Left}
        else:
            if e == At23:
              visited_g = True
              e = yield {waitFor: AnyAt}