# -*- coding: utf-8 -*-
"""task4.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1xr0ykmflWmrE8LU7N72vjRC_AkZ90BNY
"""

# Don't touch this b-thread
@thread
def bt_1():
    while True:
        yield {request: [Up, Down, Left, Right]}

# Enter your solution below
visited31 = True

@thread
def bt_2():
    e = At11
    while True:
        # don't allow moving to wall in this cell
        if e == At33:
            visited31 = False
            e = yield {waitFor: AnyAt, block: [Down, Right]}
        # when standing one next to (3,3), don't allow it if didn't visit (3,1) before (in the current interval)
        elif (e == At23) and not visited31:
            e = yield {waitFor: AnyAt, block: Down}
        elif (e == At32) and not visited31:
            e = yield {waitFor: AnyAt, block: Right}
        else:
            if e == At31:
                visited31 = True
            e = yield {waitFor: AnyAt}