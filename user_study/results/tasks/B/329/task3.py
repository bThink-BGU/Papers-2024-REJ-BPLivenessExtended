# -*- coding: utf-8 -*-
"""task3.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1i05U7S9LuriwTXAsHRCavGFZEMFk3dm5
"""

# Don't touch this b-thread
stepCounter = 0;
@thread
def bt_1():
    for i in range(5):
        yield {waitFor: [Up, Down, Left, Right]}
        stepCounter+=1
    yield {block: [Up, Down, Left, Right]}

# Enter your solution below

@thread
def bt_3():
  e = At11
  while True:
    if e == At12 and stepCounter == 1:
      e = yield {waitFor: AnyAt, block: [Left]}
    if stepCounter == 2:
      if e == At11:
        e = yield {waitFor: AnyAt, block: [Down]}
      if e == At31:
        e = yield {waitFor: AnyAt, block: [Up]}
    if stepCounter == 3:
      if e == At12:
        e = yield {waitFor: AnyAt, block: [Right]}
      if e == At32:
        e = yield {waitFor: AnyAt, block: [Left]}
    if stepCounter == 4:
      if e == At13:
        e = yield {waitFor: AnyAt, block: [Right]}
      if e == At33:
        e = yield {waitFor: AnyAt, block: [Left]}


@thread
#this thread makes sure the robot doest hit the wall
def bt_2():
    e = At11
    while True:
        if e == At11:
            e = yield {waitFor: AnyAt, block: [Up,Right]}
        elif e == At12 or e == At32:
            e = yield {waitFor: AnyAt, block: [Down, Up]}
        elif e == At13:
            e = yield {waitFor: AnyAt, block: [Up, Left]}
        elif e == At21 or e == At23:
            e = yield {waitFor: AnyAt, block: [Left, Right]}
        elif e == At31 :
            e = yield {waitFor: AnyAt, block: [Down, Right]}
        elif e == At33:
            e = yield {waitFor: AnyAt, block: [Down, Left]}